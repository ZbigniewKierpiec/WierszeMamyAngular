import { After5LinePipe } from './after5-line.pipe';

describe('After5LinePipe', () => {
  it('create an instance', () => {
    const pipe = new After5LinePipe();
    expect(pipe).toBeTruthy();
  });
});
