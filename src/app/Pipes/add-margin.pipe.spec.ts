import { AddMarginPipe } from './add-margin.pipe';

describe('AddMarginPipe', () => {
  it('create an instance', () => {
    const pipe = new AddMarginPipe();
    expect(pipe).toBeTruthy();
  });
});
